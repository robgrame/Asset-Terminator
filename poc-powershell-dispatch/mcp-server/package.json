{
  "name": "asset-terminator-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server exposing the Asset-Terminator disposal Function API (wipe intake + status) as tools for AI agents.",
  "type": "module",
  "bin": {
    "asset-terminator-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "prepare": "npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
